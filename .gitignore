node_modules
dist
uploads/

# Secrets — never push these to GitHub (set vars in Railway instead)
.env
.env.local
.env.*.local

# Replit editor tooling (huge rules file — not needed for Railway)
.config/replit/
.DS_Store
server/public
vite.config.ts.*
*.tar.gz