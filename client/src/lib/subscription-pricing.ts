/** Expected Stripe recurring amounts (USD) — server also infers tier from ~$50 vs ~$500 when product id is unknown. */
export const PRO_MONTHLY_USD = 50;
export const MENTORING_MONTHLY_USD = 500;

/** CRM / UI tier price labels (USD per month). */
export const TIER_PRO = 50;
export const TIER_MENTOR = 500;
