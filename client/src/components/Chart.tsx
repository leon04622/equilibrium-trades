/**
 * Trading chart entry — implementation lives in `pattern-chart.tsx` (candles, cache, SMMA worker).
 * Import `Chart` when you want the performance-hardened Hyperliquid chart by that name.
 */
export { PatternChart as Chart, PatternChart } from "@/components/pattern-chart";
